# Wird NICHT committed. In .gitignore.
# In Prod via: docker compose --env-file <(infisical export ...) up -d
# Lokal via:  export $(grep -v '^#' .env | xargs) && docker compose up -d

ANKI_DB_ROOT_PASSWORD=<token...ken>
ANKI_DB_PASSWORD=<token...ken>
ANKI_DB_TOKEN=<token...ken>
ANKI_SYNC_API_TOKEN=<token...ken>
ANKI_MCP_TOKEN=<token...ken>
ANKI_SYNC_IMAGE=ghcr.io/icem0/anki-sync-api:latest
